[package]
name = "shinden-to-anilist-driver"
version = "0.1.0"
edition = "2024"
license = "MPL-2.0"

[lib]
crate-type = ["cdylib", "staticlib", "rlib"]

[dependencies]
shinden-to-anilist-core = { path = "../shinden-to-anilist-core" }
